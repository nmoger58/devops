services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    restart: always
    env_file:
      - ./backend/.env
    
  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    restart: always
    env_file:
      - ./frontend/.env
    depends_on:
      - backend